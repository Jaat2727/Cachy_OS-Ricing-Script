# Hyprland color variables — generated by Matugen
# $image is set once for hyprlock/other consumers
$image = {{image}}

<* for name, value in colors *>
${{name}} = rgba({{value.default.hex_stripped}}ff)
<* endfor *>
